
const soma = {
  soma: function (a, b) {
    console.log(a + b);
  },

  
    multiplica: function (a, b) {
      console.log(a * b);
    },
  
    subitrai: function (a, b) {
      console.log(a - b);
    }
};

module.exports = soma;
